
  # Design app with logos (Copy)

  This is a code bundle for Design app with logos (Copy). The original project is available at https://www.figma.com/design/eJo7EwXwJV4G4dPl0vHZ2y/Design-app-with-logos--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  