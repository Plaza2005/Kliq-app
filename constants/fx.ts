// Flat design — no glow or shadow effects
export const glowBox = (_color: string, _size?: number): object => ({});

export const glowBorder = (color: string) => ({
  borderColor: color,
  borderWidth: 1,
});

export const glowText = (_color: string): object => ({});
